{
    "extends": "@pentible/tsconfig",
    "compilerOptions": {
        "outDir": "dist",
        "baseUrl": ".",
        "paths": {
            "~/*": ["./src/*"]
        },
        "sourceMap": true,
        "declarationMap": true,
        // TODO: fix error: The inferred type of 'api' cannot be named without a reference to '[...]node_modules/@trpc/react-query/dist/createTRPCReact
        "moduleResolution": "Node10",
        // next requirements
        "resolveJsonModule": true,
        "jsx": "preserve",
        "plugins": [{ "name": "next" }],
        // NOTE: next adds automatically, but these aren't required
        "noEmit": true,
        "lib": ["dom", "dom.iterable", "esnext"],
        "incremental": true,
        "module": "esnext",
        "strictNullChecks": true
    },
    // NOTE: next adds automatically, but these aren't required
    "include": ["next-env.d.ts", "**/*.ts", "**/*.tsx", ".next/types/**/*.ts"],
    "exclude": ["node_modules"]
}
